import React, { Component } from "react";
import { Jumbotron, Container } from "react-bootstrap";
import Login from "../Login/Login";

export default class LoginJumbo extends Component {
  render() {
    return (
      <div>
        <Login />
      </div>
    );
  }
}
